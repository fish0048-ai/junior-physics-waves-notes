---
description: 段考與章末評量題庫格式、附圖、壞題清理。編輯 exams 或 exam-*.js 時套用。
globs: exams/**/*.html, js/exam*.js, review*.html
alwaysApply: false
---

# 段考與章末評量

新章跟第 4 章：不要再用 `exam-bank.js`、`exam-sections.js`、`exam-figs.js`。

實驗專區不要套用本規則：不要 `exams/lab-*.html`、不要 `review-lab.html`、不要 `js/exam-lab*.js`。見 `labs.md`。

**匯入題庫**：人工交 HTM＋答案／解析；AI 轉 JS／拷圖。分工見 `jpwn-workflow.mdc`、`workflow.md` §4。禁止解壓 docx、禁止瞎編答案。

## 檔案

- 小節頁 `exams/N-x.html` 載入：config-chN、layout、app、exam-chN-sections、exam-chN-keys、exam.js
- 章末 `review-chN.html` 載入：config-chN、layout、app、exam-chN、exam.js；`data-pick="40"`
- 題庫 `js/exam-chN-sections.js` → `EXAM_BANK_SECTIONS["N-1"]` 陣列
- 章末 `js/exam-chN.js` → `EXAM_BANK_SECTIONS["ch-N"]`，約 120 題
- 答案帶 `js/exam-chN-keys.js`：字串長度必須等於該節題數，否則整段不覆寫

## 一題

```js
{ q: "題幹", choices: ["A","B","C","D"], ans: 0, explain: "詳解",
  imgs: ["exams/img/N-1/image001.png"], choiceImgs: [], lead: "", group: "" }
```

`ans` 是 0–3。附圖路徑從站根起算 `exams/img/…`。一題圖 ≤2，選項圖 ≤4。題幹裡的公式用 `\(...\)`，`exam.js` 注入後會再跑 KaTeX。

## 轉檔後必清

丢掉：空題、過短殘句（「會考〕」）、複選黏題、選項少於 2 且沒有選項圖。`exam.js` 的 `sanitizeItem`／`isJunkStem` 會再濾一次，不要靠它當唯一品管。

## 章末題本

橘色班級列可「存成此班題本」。換一套題不會自動覆蓋，要再按存檔。localStorage 鍵 `${classId}::${secId}`。
