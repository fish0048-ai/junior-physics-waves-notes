---
description: 探究教學怎麼寫進講義：問題、路徑、線索、銜接、主張、練習。
globs: sections/**/*.html, index.html, ch*.html
alwaysApply: false
---

# 探究寫法

精神：現象進教室 → 學生先預測 → 用本節定義／圖／實驗當證據 → 形成主張 → 用練習檢驗。

## 問題要像課堂第一句

- 好：泳池為什麼看起來比較淺？
- 不好：4-3 光的折射與透鏡

一節一個主問題，寫進 hero、`#inquiry` 標題、`config.sections[].ask`。主張必須能被本節證據檢驗。

## 每一節固定順序

hero 探究問題 → toc → `#inquiry` → 各重點（ask→原文→國中程度 extra→國中資優 gifted→bridge）→ 延伸／實驗／計算 → `#memo` 證據卡 → `#claim` → `#drill` 檢驗主張。

實驗專區以步驟為上課主軸：探究 → 目的／器材 → **逐步操作（`.lab-step` 一格一步，禁止收成 `ol.q`）** → 紀錄表 → memo／claim。假設／器材才可用 `ol.q`。**不要** `#drill`、不要段考前練習、不要章末評量。見 `labs.md`。

## 章首

hero 用箭頭串整章。inquiry 卡寫**一段故事**，不要再列一排小節連結（卡片由 `layout.js` 用 `ask` 產生）。

## 自動填答（不要手做）

`app.js` 會在 `.inquiry-lead` 後插入「我的預測」，在「探究線索」的 `.inquiry-ask` 後插入「這一站我先寫」，在 `.inquiry-claim` 插入「我的主張」並把參考主張藏到「顯示答案」。標題含「證據卡」「檢驗主張」的 ask **不要**當預測空位。

## 禁止

- 不要單獨放 `.overview` 重點概覽（併進探究卡副標）。
- 加 `inquiry-bridge` 時，下一個 `<article id="…">` 的開頭標籤必須還在。
- 不要刪原定義去改寫成問句；問句加在前面。
- 重點卡課文後必備 `.extra`（國中程度）與 `.gifted`（國中資優），見 `extras.md`。
