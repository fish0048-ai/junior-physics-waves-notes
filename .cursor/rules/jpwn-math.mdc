---
description: 數學公式與座標圖必須用 KaTeX 學術級精準顯示。改講義、段考、實驗、公式、關係圖時必套用。
globs: "{sections,exams,js,css}/**/*.{html,js,css}"
alwaysApply: true
---

# 數學公式：學術級精準顯示（不可破）

細節與範例見技能 `.cursor/skills/junior-physics-notes/math.md`。KaTeX 由 `js/layout.js` 的 `JPWNMath` 載入；段考注入後必須再 `JPWNMath.render()`（`exam.js` 已做）。可用 `JPWNMath.polish(text)` 把題庫破公式轉成 KaTeX。

## 硬規則

1. **獨立公式**放 `.formula`，內容寫 LaTeX `\[ ... \]`。頁面必須顯示 **KaTeX 排版**（分數橫線、斜體變數），**禁止**學生看到 `\[v=\dfrac{s}{t}\]` 原始碼字串。
2. **一行一式**：同一個 `.formula` 不要用 `<br>` 串兩條 `\[...\]`；要分開兩個 `.formula`（渲染器雖可拆多條，HTML 仍以一格一式為準）。
3. **行內**用 `\(...\)`，例如 \(D=\dfrac{M}{V}\)、\(v=f\lambda\)。不要用 `<em>V</em>＝…` 冒充公式。
4. `JPWNMath.render()`／`scheduleRender()` 必須成功；`.formula` 用 `katex.render` 直接排版（會保存 `data-math-src`，支援同格多條）。
4. **分數**用 `\dfrac`（獨立）或 `\tfrac`／`\dfrac`（行內）；**乘**用 `\,` 或 `\times`；**等號** `=`；**減號** `-` 或 `\minus`；指數用 `^{}`（`\(\mathrm{cm}^3\)`、`\(10^{-9}\)`）。
5. **禁止當正式公式**：`D ＝ M／V`、`g/cm 3`、`cm 3`、`10－9m`、`H = M x ΔT`、`1/2 * v * t`、全形 `＝／×÷`、把單位寫成 `cm3`／`g/cm3` 卻不排成上標。
6. **挖空**不要包進 `\(...\)`／`\[...\]`，否則 KaTeX 會吃掉 `input.blank`。
7. **段考／題庫**：題幹、選項、題組 lead、解析裡的密度、體積、指數、溫度公式都要能被 KaTeX 正確顯示；轉檔後若出現 `g/cm 3`、`10－9` 等，要修源檔或經 `JPWNMath.polish` 再渲染。
8. **化學式**可用 HTML `<sub>`（如 H<sub>2</sub>O）或 KaTeX `\mathrm{H_2O}`，同一頁風格一致即可。

## 座標圖

`svg.diagram.math-graph` 必有：原點 **O**、軸箭頭、軸名（斜體變數＋直立單位）、刻度、格線、圖例、`.caption`。座標比例要算準。範本：`sections/1-2.html`、`sections/5-3.html`。

光線／器材／原子模型示意不是座標圖，維持普通 SVG 或照片，但標的物理量仍用斜體符號。

## 完成前自查

- [ ] 每個 `.formula` 內是 `\[...\]`，預覽可見排版（分數線、上標、希臘字母）
- [ ] 行內物理量不是「純文字貼上」
- [ ] 單位無 `cm 3`／`g/cm 3` 這種被空格拆開的上標
- [ ] 段考頁 `JPWNMath.render()` 後公式正確
