---
description: 講義 PDF／列印：先保版面正確；頁碼用 @page；禁止 absolute 疊層切版。
alwaysApply: true
---

# PDF 列印與頁碼（不可破）

細節見 `.cursor/skills/junior-physics-notes/pages.md`。

## 版面優先

1. `@page { size: A4; margin: 12mm 12mm 16mm; }` 保留正常頁邊。
2. **禁止**用 absolute／fixed 沿高度每張 A4 疊頁碼（會切字、號碼跑到奇怪位置）。
3. **禁止**為了藏網址而設 `@page { margin: 0 }` 卻沒把留白還給內容。
4. 瀏覽器左下角路徑：提示使用者取消「頁首與頁尾」。

## 編號

- 清單：`js/book-manifest.js`／`JPWNPages`（封面不編；1-0 起算第 1 頁）
- 螢幕：側欄／章首顯示起始頁即可
- PDF：`@page @bottom-center` + `counter(page)`；`print-folios.js` 的 install 保持 no-op
