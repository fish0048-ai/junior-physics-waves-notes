---
description: 講義 PDF／列印頁碼：依整本順序編號，固定頁底置中；螢幕僅作導覽提示。
alwaysApply: true
---

# PDF 頁碼（不可破）

頁碼**以下載 PDF／瀏覽器列印為主**，不是螢幕閱讀裝飾。細節見技能 `.cursor/skills/junior-physics-notes/pages.md`。

## 編號來源

- 唯一清單：`js/book-manifest.js`（各章小節 → 實驗專區；**封面不編頁**）
- API：`JPWNPages`（`build`／`lookup`／`rangeForPack`）
- 新節／新實驗必須寫進 `packs` 或 `labs.files`，頁碼才會連續正確
- **封面**（`cover.html`／整本裡的封面段）**永不印頁碼**，也不佔編號；內文從第 1 頁起（例如 1-0＝第 1 頁）

## 顯示規則

1. **PDF／`@media print`**：`.page-folio` 必須出現，且用 **`position: fixed`** 固定在**每張紙底部置中**（`bottom: 6mm`、左右拉滿、置中）。`@page` 下緣留白至少約 `16mm`，避免內文蓋住頁碼。
2. **螢幕**：`.page-folio` **隱藏**（`display: none`）。側欄／章首卡片可顯示頁碼作導覽，不要在頁尾再大字重複。
3. **單節下載 PDF**：頁碼為該節在整本中的編號（如 1-1 → `— 3 —`），同一節多張紙可重複同一編號。
4. **整本／分章 PDF**（`book-pdf.js`）：整份文件只用**一顆** `.page-folio.page-folio-running`，以 `counter(page)` 印實體張號；各節 `book-kicker` 可保留「第 N 頁」對照整本目錄。禁止每個 `.book-section` 各放一顆 fixed folio（Chrome 會疊在每一頁）。
5. **不要**把頁碼寫死在各 HTML；由 `layout.js`／`book.js`／`book-pdf.js` 注入。
6. **封面無頁碼**：`data-page="cover"`、整本 `.book-section.is-cover`、只下載封面時，都不可出現 `.page-folio`。

## 禁止

- 把 `.page-folio` 放回文章 flow（只出現在最後一張）
- 螢幕與 PDF 搶同一套大頁碼排版
- 發明第二套頁碼清單（不經 `book-manifest.js`）
