---
description: 講義 PDF／列印頁碼：依整本順序編號，固定頁底置中；螢幕僅作導覽提示。
alwaysApply: true
---

# PDF 頁碼（不可破）

頁碼**以下載 PDF／瀏覽器列印為主**，不是螢幕閱讀裝飾。細節見技能 `.cursor/skills/junior-physics-notes/pages.md`。

## 編號來源

- 唯一清單：`js/book-manifest.js`（各章小節 → 實驗專區；**封面不編頁**）
- API：`JPWNPages`（`build`／`lookup`／`rangeForPack`）
- 新節／新實驗必須寫進 `packs` 或 `labs.files`，頁碼才會連續正確
- **封面**（`cover.html`／整本裡的封面段）**永不印頁碼**，也不佔編號；內文從第 1 頁起（例如 1-0＝第 1 頁）

## 顯示規則

1. **PDF／列印**：頁碼在每張紙底部置中，且**同節多頁必須遞增**（由 `js/print-folios.js` 依內容高度每隔一張 A4 放置號碼；起始頁來自 `JPWNPages`）。
2. **關掉瀏覽器路徑頁尾**：`@page { margin: 0 }`，留白改放在 `.wrap`／`.book-section` 的 padding（約 `8mm 10mm 16mm`）。否則 Chrome／Edge 會在左下角印 `file://…` 或網址。
3. **螢幕**：側欄／章首卡片可顯示該節**起始頁**；不要用單顆 fixed 大頁碼。
4. **禁止**用一顆 `position: fixed` 寫死 `— N —` 蓋在整節所有紙上（會造成「每一節裡面的頁碼都一樣」）。
5. **整本／分章 PDF**（`book-pdf.js`）：封面段無頁碼；內文 `.book-body` 安裝 folio stack。
6. **不要**把頁碼寫死在各 HTML；由 `layout.js`／`print-folios.js`／`book-pdf.js` 注入。
7. **封面無頁碼**：`data-page="cover"`、整本 `.book-section.is-cover`、只下載封面時，都不可出現頁碼。

## 禁止

- 把 `.page-folio` 放回文章 flow（只出現在最後一張）
- 螢幕與 PDF 搶同一套大頁碼排版
- 發明第二套頁碼清單（不經 `book-manifest.js`）
